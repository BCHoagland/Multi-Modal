# Multi-Modal

## Dependencies
* NumPy
* Visdom
* PyTorch


## Running the Code
**Terminal 1:**
Start a local Visdom server with the command `visdom`

**Terminal 2:**
`cd` into the repo and then run `python train.py`
